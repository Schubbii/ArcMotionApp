import { registerRootComponent } from "expo";
import App from "./App";

// registerRootComponent calls AppRegistry.registerComponent and sets up the
// environment for both Expo Go and native builds.
registerRootComponent(App);
